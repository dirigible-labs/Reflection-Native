'use strict';

const React = require('react-native');

const {
    View,
    Text,
} = React;

const DataScreen = React.createClass({

    render() {
        return (
            <View><Text>DataScreen</Text></View>
        );
    }
});

module.exports = DataScreen;
